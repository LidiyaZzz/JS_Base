alert(`hw3. Урок 6. Семинар. Знакомство с функциями в JavaScript`);

/* Задание 1
Создайте функцию которая возводит переданное число в куб, необходимо вывести в консоль результат 2^3 степени + 3 ^ 3 степени
*/

// Создаем функцию для возведения числа в куб
function cube(number) {
    return number ** 3;
}

// Вычисляем результат 2^3 + 3^3
let result = cube(2) + cube(3);

// Выводим результат в консоль
console.log(result);

/*
Задание 2
Пользователь вводит с клавиатуры число, если ввёл текст, необходимо вывести что значение задано неверно
Создать фукнцию, которая высчитывает 13% от данного числа и выводит в консоль текст "Размер заработной платы за вычетом налогов равен значение"
*/
// Получаем число от пользователя с клавиатуры
let userInput = prompt("Введите размер заработной платы ДО вычета налогов:");

// Проверяем, является ли введенное значение числом
if (!isNaN(userInput)) {
    let number = parseFloat(userInput);
    
    // Создаем функцию для вычисления 13% от числа
    function calculateSalary(number) {
        let tax = number * 0.13;
        let salaryAfterTax = number - tax;
        console.log(`Размер заработной платы за вычетом налогов равен ${salaryAfterTax}`);
    }

    // Вызываем функцию и выводим результат в консоль
    calculateSalary(number);
} else {
    console.log("Значение задано неверно. Пожалуйста, введите число.");
}

/*
Задание 3
Пользователь с клавиатуры вводит 3 числа, необходимо создать функцию, которая определяет максимальное значение среди этих чисел
*/

// Получаем три числа от пользователя с клавиатуры
alert(`Задание 3: определить максимальное значение среди этих чисел`);
let num1 = parseFloat(prompt("Введите первое число:"));
let num2 = parseFloat(prompt("Введите второе число:"));
let num3 = parseFloat(prompt("Введите третье число:"));

// Создаем функцию для определения максимального значения
function findMax(num1, num2, num3) {
    return Math.max(num1, num2, num3);
}

// Вызываем функцию и выводим результат в консоль
let maxNumber = findMax(num1, num2, num3);
console.log(`Максимальное значение среди введенных чисел: ${maxNumber}`);

/*


Задание 4
Необходимо реализовать четыре функции, каждая функция должна принимать по два числа и выполнять одну из операций (каждая функция выполняет одну из них):
1. Сложение
2. Разность
3. Умножение
4. Деление
Необходимо сделать так, чтобы функция вернула число, например выражение console.log(sum(2, 6)); должно вывести число 8 в консоль (sum - функция сложения в данном примере, ваши названия функций могут отличаться). Округлять значения, которые возвращают функции не нужно, однако, обратите внимание на разность, функция должна вычесть из большего числа меньшее, либо вернуть 0, если числа равны. Функциям всегда передаются корректные числа, проверки на NaN, Infinity делать не нужно.
*/

// Функция для сложения двух чисел
function sum(a, b) {
    return a + b;
}

// Функция для вычитания двух чисел
function subtract(a, b) {
    return a > b ? a - b : b - a;
}

// Функция для умножения двух чисел
function multiply(a, b) {
    return a * b;
}

// Функция для деления двух чисел
function divide(a, b) {
    return b !== 0 ? a / b : "Деление на ноль!";
}

// Примеры использования функций
console.log(sum(2, 6)); // Вывод: 8
console.log(subtract(10, 4)); // Вывод: 6
console.log(multiply(3, 5)); // Вывод: 15
console.log(divide(8, 2)); // Вывод: 4