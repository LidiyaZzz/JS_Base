alert(`hw2. Урок 4. Семинар. Основы JavaScript`);
/* 
Задание 1

Создать переменные num1, num2 которые пользователь вводит с клавиатуры
Проверьте, что переменная num1 равна или меньше 1, а переменная num2 больше или равна 3.
*/

// Задаем пользователю ввести значения для переменных num1 и num2 с клавиатуры
let num1 = parseFloat(prompt("Введите значение для переменной num1 так, чтобы num1 было <= 1:"));
let num2 = parseFloat(prompt("Введите значение для переменной num2 так, чтобы num2 было >= 3:"));

// Проверяем условия: num1 <= 1 и num2 >= 3
if (num1 <= 1 && num2 >= 3) {
    // Выводим переменные num1 и num2 в консоль, если условия выполняются
    alert(`Переменная num1: ${num1}`);
    alert(`Переменная num2: ${num2}`);
} else {
    alert(`Условия не выполнены, одно или оба значения введены не верно.`);
}


/* 
Задание 2

Перепишите код к тернарному оператору

let test = true;
if (test === true) {
console.log('+++');
} else {
console.log('---');
}
*/

let test = true;
test === true ? console.log('+++') : console.log('---');

/* 
Задание 3

В переменной day лежит какое-то число из интервала от 1 до 31. Определите в какую декаду месяца попадает это число (в первую, вторую или третью).
*/

let day = 15; // Пример числа из интервала от 1 до 31

if (day >= 1 && day <= 10) {
    console.log("Число попадает в первую декаду месяца.");
} else if (day > 10 && day <= 20) {
    console.log("Число попадает во вторую декаду месяца.");
} else if (day > 20 && day <= 31) {
    console.log("Число попадает в третью декаду месяца.");
} else {
    console.log("Число не входит в диапазон от 1 до 31.");
}

/* 
Задание 4

Необходимо от пользователя получить число.
Необходимо вывести разряды числа, а именно: количество сотен, десятков и единиц.

Пример:
Пользователь ввел число 163. Программа должна вывести:
"В числе 163 количество сотен: 1, десятков: 6, единиц: 3"

Пример 2:
Пользователь ввел число 74. Программа должна вывести:
"В числе 74 количество сотен: 0, десятков: 7, единиц: 4"

Пример 3:
Пользователь ввел число 9537. Программа должна вывести:
"В числе 9537 количество сотен: 5, десятков: 3, единиц: 7"

Уточнение: пользователь всегда вводит корректное положительное целое число.
*/

// Получаем число от пользователя
let number = parseInt(prompt("Введите число:"));

// Вычисляем количество сотен, десятков и единиц
let hundreds = Math.floor(number / 100);
let tens = Math.floor((number % 100) / 10);
let units = number % 10;

// Выводим результат
alert(`В числе ${number} количество сотен: ${hundreds}, десятков: ${tens}, единиц: ${units}`);